# Reviewed English typo contaminants removed from the frequency dictionary at load time.
# These are frequent misspellings that leaked into the corpus and would otherwise be treated
# as valid rare words, blocking their correction. One lookup-form word per line; '#' = comment.
# Keep this list conservative: only clear, unambiguous typos — never real (if rare) words,
# and never informal contractions people actually type (dont, cant, wont, ...).
teh
thier
alot
