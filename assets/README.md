# Assets

Put an optional custom alert sound here, for example `alert.wav`.

The app works without this file and uses the built-in Windows exclamation sound by default. Users can also choose any WAV file from the settings panel.

